import Playlist from '../../types/playlist'

type PlaylistShowViewProps = {
  playlist: Playlist
}

const PlaylistShowView = (props: PlaylistShowViewProps) => {
  return(
    <div>fafa</div>
  )
}

export default PlaylistShowView
